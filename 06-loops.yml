- name: Demo on loops
  hosts: localhost
  connection: local
  tasks:
    - name: Print fruit name
      ansible.builtin.debug:
        msg: Fruit name - {{ item }}
      loop:
        - apple
        - grape
        - banana
        - orange
